<h1>All Lists</h1>
<%= link_to 'Add a new list', new_list_path %>
<ul>
  <% @lists.each do |list| %>
    <li><%= link_to list.name, root_path(list) %></li>
  <% end %>
</ul>

<%= link_to 'New List', new_list_path %>
